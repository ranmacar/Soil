# Map Tiles API key for Google Photorealistic 3D Tiles.
# Enable "Map Tiles API" in Google Cloud, then:
#   cp .env.example .env
# Restart `npm run dev`. You can also pass ?key= on the URL for a one-off test.
VITE_GOOGLE_TILES_API_KEY=
